services:
  app:
    build: .
    command: uvicorn hope_job_agent.main:app --host 0.0.0.0 --port 8000 --reload
    env_file:
      - .env.example
    ports:
      - "8000:8000"
    volumes:
      - ./src:/app/src

